# This folder is reserved for credentials exported from n8n.
#
# IMPORTANT: Credentials contain sensitive information (API keys, passwords).
# By default, this folder contains the credentials for *Qdrant* and *Ollama*, which are the containerized services required for the demo.
# Configure your credentials manually from the n8n interface after the first startup:
# Overview > Credentials > Add Credential